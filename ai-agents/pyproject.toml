[project]
name = "ai-agents"
version = "0.1.0"
description = "Multi-agent AI pipelines for Social Media Manager"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
